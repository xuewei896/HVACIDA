/* HVACIDA UI 原型 —— 交互流程图 / 交互逻辑表 数据文件(可选)
 * ---------------------------------------------------------------------------
 * 优先级:浏览器本机编辑(localStorage) > 本文件 > ui.js 内置默认
 *
 * 用法:
 *   1) 打开 docs/ui-prototype/index.html,点顶部「✎ 编辑流程图/逻辑表」;
 *   2) 直接改文字(节点右上角可改类型/删除,泳道末尾「+ 节点」,表下「+ 添加一行」);
 *   3) 点「导出/导入数据…」→「复制全部」;
 *   4) 把复制到的整段内容**覆盖粘贴到本文件**(替换下面这段注释即可),保存后刷新页面。
 *
 * 想清空本机编辑、改用本文件内容:编辑数据窗里点「丢弃本机编辑」。
 *
 * 数据格式(导出即为此格式,可直接手改):
 *   window.HVACIDA_FLOW = [
 *     { h: '① 项目信息',                              // 可选:该行的小标题
 *       items: [
 *         { t: 'win', x: '项目信息窗口' },            // 节点:t=类型, x=文字(可含 <br>)
 *         { t: 'arrow' },                             // 横向箭头
 *         { t: 'act', x: '手动填写 / Excel 模板导入' }
 *       ] },
 *     { items: [ { t: 'mod', x: '大系统负荷计算<br><small>2.2.3.1</small>' } ] }   // 无 h 的块
 *   ];
 *   节点类型 t:root(入口) / mod(板块) / win(窗口) / act(操作) / sys(未实现)
 *
 *   window.HVACIDA_LOGIC = {
 *     table: [ ['点击 Ribbon 按钮', 'HVACIDA 页', '触发与反馈', '异常/边界处理'] ],
 *     units: [ ['温度 / 温差', '℃', '1', '19.0'] ]
 *   };
 *
 * 本文件默认全部注释掉 → 页面使用 ui.js 内置默认数据。
 * ---------------------------------------------------------------------------
 */

// window.HVACIDA_FLOW = [ ... ];
// window.HVACIDA_LOGIC = { table: [ ... ], units: [ ... ] };
